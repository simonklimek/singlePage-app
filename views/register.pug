 div.row
    div.col.m6.s12
        h1 REGISTER
        form.col.s12(method="POST", action="/users/registration")
            div.row
            div.input-field.col.s12
                input.validate(id="first_name", type="text")
                label(for="first_name") Login
            div.input-field.col.s12
                input.validate(id="password", type="password")
                label(for="password") Password
            div.input-field.col.s12
                input.validate(id="email2", type="email")
                label(for="email2") Password 
            button.btn.waves-effect.orange.waves-light(type="submit", name="action") Register
                i.material-icons.right check
    div.col.m6.s12
        h1 LOGIN
        div.row
        form.col.s12(method="POST", action="/users/login")
            div.row
                div.input-field.col.s12
                    i.material-icons.prefix account_circle
                    input.validate(id="icon_prefix", type="text")
                    label(for="icon_prefix") Login